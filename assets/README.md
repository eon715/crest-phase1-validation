# Assets

This folder is reserved for figures, tables, and regenerated reproducibility outputs associated with the CREST Phase 1 technical document.

The current asset bundle is stored as:

- `CREST_Phase1_Reproducibility_Assets.zip`

Future versions should unpack the asset bundle into:

- `figures/`
- `tables/`
- `workflow/`
